{
  "name": "linkvault",
  "version": "1.0.0",
  "description": "LinkVault - Smart Link Sharing Platform",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm ci && npm run build && cp -r dist ../backend/public",
    "start": "cd backend && npm start",
    "install:all": "cd backend && npm ci && cd ../frontend && npm ci"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
